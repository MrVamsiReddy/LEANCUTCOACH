import SwiftUI

enum Palette {
    static let backgroundTop = Color(red: 0.08, green: 0.12, blue: 0.14)
    static let backgroundBottom = Color(red: 0.04, green: 0.25, blue: 0.27)
    static let accent = Color(red: 0.94, green: 0.56, blue: 0.30)
    static let accentSoft = Color(red: 0.98, green: 0.77, blue: 0.55)
    static let success = Color(red: 0.54, green: 0.85, blue: 0.71)
    static let card = Color.white.opacity(0.09)
    static let stroke = Color.white.opacity(0.12)
    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.72)
    static let chip = Color.black.opacity(0.18)
}
